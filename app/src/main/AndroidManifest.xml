<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michael.customizedlvdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".dialogstyle.single.MyRBActivity" />-->
        <!--<activity android:name=".dialogstyle.multiple.MyCBActivity" />-->
        <activity android:name=".qqstyle.QQStyleActivity" />
        <activity android:name=".checkboxstyle.vertical.CBVerticalListActivity" />
        <activity android:name=".checkboxstyle.horizontal.CBHorizontalListActivity" />
        <activity android:name=".dialogstyle.MainDialogActivity" />
        <!--<activity android:name="com.michael.customizedlvdemo.MainActivity" />-->
        <!--<activity android:name=".dialogstyle.MyCBActivity" />-->
    </application>

</manifest>